# 淘宝女装 AI 智能剪辑 Agent 发展规划

## 1. 产品定位

当前产品是“Web 前端 + Python 后端 + SQLite 数据库 + ASR/视觉/FFmpeg 剪辑流水线”。下一阶段不把网页或固定规则改名为 Agent，而是增加一个能根据真实证据循环工作的总控 Agent：

`理解任务 → 调用工具分析素材 → 制定剪辑计划 → 执行剪辑 → 质量评估 → 修正计划并重剪 → 合格或转人工复核 → 保存反馈`

对外名称：**淘宝女装 AI 智能剪辑 Agent**。

## 2. Agent 的落地边界

### Agent 负责

- 根据口播、动作、画面质量和商品资料判断品类与素材完整度。
- 从候选时间段中制定可解释的剪辑计划。
- 根据质检失败项调整计划，最多自动重剪两次。
- 记录每次工具调用、证据、计划、失败原因和人工反馈。
- 在证据不足、连续失败或涉及发布时转交人工确认。

### 确定性程序负责

- ASR 时间戳、姿态/景别/稳定性检测、黑帧与闪现检测。
- 3:4 输出、原速、原声、无字幕、无 BGM 等硬约束。
- FFmpeg 渲染、文件校验、时长校验和质量门。
- 用户权限、数据库写入、文件隔离和审计日志。

### Agent 禁止

- 凭空补充素材中不存在的尺码、面料、颜色、动作或时间戳。
- 绕过质量门直接宣布合格。
- 为凑时长变速、重复片段、生成字幕、配音或音乐。
- 删除原片、覆盖人工成片或自动发布商品视频。

## 3. 第一版工具箱

| 工具 | 输入 | 输出 | 作用 |
|---|---|---|---|
| `probe_video` | 视频路径 | 编码、尺寸、FPS、时长、音轨 | 素材体检 |
| `transcribe_speech` | 视频路径 | 带时间戳口播句子 | 抽取尺码、面料、版型等语义 |
| `analyze_model_actions` | 视频路径 | 全身、靠近、细节、转身、背面、回正候选 | 动作证据 |
| `detect_visual_defects` | 视频路径/区间 | 黑帧、闪现、曝光、景别突跳 | 排除异常片段 |
| `retrieve_edit_rules` | 品类、任务信息 | 当前版本规则与历史合格案例 | 知识库检索 |
| `build_edit_plan` | 所有证据 | 结构化时间轴计划 | 决策与解释 |
| `validate_plan` | 计划、证据 | 错误列表 | 渲染前拦截虚构和冲突 |
| `render_video` | 视频、计划 | MP4 与渲染报告 | FFmpeg 确定性执行 |
| `inspect_output` | 成片、计划 | 分项评分与失败原因 | 成片质量门 |
| `revise_plan` | 原计划、失败原因 | 新计划 | 有限自动重剪 |
| `save_feedback` | 人工评价、时间轴 | 结构化反馈 | 形成长期记忆 |

工具优先做成 Python 函数和 REST API。等接口稳定后，再封装成 MCP 服务，方便未来让不同模型、Dify 或其他 Agent 平台复用；不要先为使用 MCP 而重写稳定功能。

## 4. 单 Agent 架构

第一阶段只使用一个“剪辑总控 Agent”，避免多个 Agent 相互传话造成成本、延迟和错误叠加。

总控 Agent 状态机：

1. `RECEIVED`：收到任务，冻结原片并生成校验值。
2. `ANALYZING`：并行调用 ASR、动作分析、画质检测。
3. `PLANNING`：检索规则和合格案例，生成结构化计划。
4. `VALIDATING`：检查时间戳、顺序、覆盖、时长和动作连续性。
5. `RENDERING`：使用 FFmpeg 原速原声渲染。
6. `EVALUATING`：检查输出画幅、时长、音频、闪现、内容覆盖和动作顺序。
7. `REVISING`：仅根据失败项更换候选区间，最多两次。
8. `PASSED`：全部硬门通过，可供负责人确认。
9. `NEEDS_REVIEW`：证据不足或重试耗尽，列明具体原因。

## 5. 数据库与记忆

新增或完善以下数据对象：

- `agent_runs`：一次任务的开始、结束、状态、耗时、模型和规则版本。
- `tool_calls`：每次工具的参数摘要、返回摘要、耗时与错误。
- `edit_plans`：每一轮剪辑计划及其证据，不覆盖旧版本。
- `quality_results`：每个质量门的通过状态、数值和失败原因。
- `human_feedback`：通过、不通过、问题类型、人工修改时间轴和备注。
- `rule_versions`：规则版本、启用状态和生效时间。
- `golden_examples`：原片、合格成片、人工时间轴、品类和适用条件。

“记忆”不是让模型自由回忆，而是从经过负责人确认的结构化数据中检索相似案例。未确认的自动成片不得进入合格案例库。

## 6. 质量门与停止条件

硬门：

- 输出固定 1080×1440（3:4），帧率与原素材兼容。
- 视频速度 1.0×，保留原声，不添加字幕、BGM、配音或滤镜。
- 单品小件不超过 30 秒；套装/两件套不超过 60 秒；大货不超过 120 秒。
- 时间轴不得越界、重叠、倒序或截断关键口播半句。
- 首幕必须有真实全身正面证据。
- 原片存在的尺码、面料、版型、颜色、背面与回正内容按规则覆盖。
- 禁止黑帧、整帧闪现、跨景别诡异跳变；同一景别下普通手势不连续不作为失败。

停止条件：

- 质量门全部通过，状态为 `PASSED`。
- 连续两次重剪仍失败，状态为 `NEEDS_REVIEW`。
- 缺少尺码/面料/背面等必要素材，不伪造，直接说明缺失。
- 工具错误、视频损坏或磁盘不足时停止，保留原片与中间报告。

## 7. 分阶段实施

### P0：可审计闭环

- 统一 Agent 状态、工具输入输出 Schema 和任务追踪日志。
- 保存每一轮计划与质量报告。
- Web 页面显示“分析—计划—剪辑—质检—重剪”的实时步骤。
- 验收：任何成片都能追溯到原片时间戳和工具证据。

### P1：自动重剪

- 将质检失败项转换为可执行的修正指令。
- 建立候选片段池，重剪只替换失败阶段。
- 最多两轮，禁止无限循环。
- 验收：至少用 20 条真实素材统计首次通过率、二次通过率和人工复核率。

### P2：人工反馈学习

- 负责人可以给成片标记问题类型，并拖动人工正确切点。
- 只将已确认案例写入合格案例库。
- 相似品类优先检索历史切法，但必须重新验证当前素材证据。
- 验收：固定测试集上的关键内容覆盖率和人工改动时长持续改善。

### P3：企业化与开放工具

- 多用户任务队列、配额、隔离存储、失败重试和审计。
- 将稳定工具封装为 MCP/REST 接口。
- 可选接入 Dify 或其他工作台，但剪辑质量门继续由本地确定性程序掌控。
- 验收：断网恢复、服务重启、并发排队、数据备份和权限隔离均有测试。

## 8. 必须持续统计的指标

- 首次质检通过率、自动重剪后通过率、人工复核率。
- 尺码、面料、颜色、背面、回正的证据覆盖率。
- 黑帧/闪现/景别突跳误报率与漏报率。
- 单条 ASR、视觉分析、规划、渲染、质检和总耗时。
- 人工平均修改时长、负责人最终采纳率。
- 每百条失败类型分布，以及规则版本前后对比。

速度目标不能替代质量目标。单条一分钟内处理作为优化指标，如实记录；不能通过跳过视觉分析或质量检查实现。

## 9. 趋势判断

- Agent 正从“聊天回答”转向“模型 + 工具 + 指令/护栏 + 运行追踪”的可执行系统。
- 对视频剪辑这类可测任务，最实用的是 Agent 负责规划与纠错、确定性工具负责执行与验收，而不是让大模型直接声称画面合格。
- MCP 适合在工具接口稳定后作为统一连接层；它是接口标准，不是剪辑质量本身。
- 企业真正购买的是通过率、节省时间、可追溯和稳定运行，不是 Agent、RAG 或大模型名称。
- 早期应保持单 Agent、有限重试和人工复核；只有当任务确实需要不同专业角色且评测证明收益时，才拆分多 Agent。

