---
system: fulin-cutting-room
version: 2
locale: zh-CN
canonical: true
token_pipeline: DESIGN.md -> web/app/globals.css / integration/suchen-ui/static/app.css -> UI components
---

# 服临 AI 剪辑台设计系统

## 产品意图

这是福州服临电子商务有限公司管理层和运营成员使用的女装直播视频自动剪辑工作台。页面的首要任务是让负责人把原视频交给真实剪辑流水线、看清当前阶段、审核成片和证据；技术架构透明展示，但不抢占主任务。桌面 SOCHEN 是主产品界面，账号、角色、剪辑记录与知识库必须来自真实后端。

主工作台默认是 Agent 入口而非普通表单：主操作明确标记 Agent，右栏持续展示有限状态机、证据、计划、质检和人工接管状态。网页是 Agent 的操作与审计界面，不将按钮点击本身描述为智能。

## 视觉方向

- 关键词：黑曜剪辑台、紫色识别光、时间码、动作轨迹、精密控制。
- 标志性元素：“品类布尺时间线”，用首秒 HARD 标记与三段缝线表现尺码、中央细节、背面回原位与转回；尺长随单品、套装、大货策略变化。
- 禁止：彩虹霓虹、玻璃拟态、无意义统计卡、过度圆角、虚构进度百分比。
- 图标：统一使用 Lucide 风格的 1.8px 线性 SVG，文字标签承担主要语义；不用 emoji 作为操作图标。
- 产品名称统一为“AI 智能剪辑”，SUCHEN 为底座标识。品牌图标直接提取桌面 `AI 智能剪辑.lnk` 所指程序的图标，运行时唯一来源为 `integration/suchen-ui/static/desktop-brand.png` 与 `.ico`；登录页、首页、浏览器标签和触摸收藏使用同一资产。账号头像和操作图标不替换成品牌图标，以保留功能区分。

SOCHEN 1.2.3 内嵌页采用黑紫色主视觉：黑色表面承载素材和时间线，紫色只用于主要操作、焦点和 AI 正在识别的状态。登录页用一条“尺码识别 → 商品细节 → 背面转回”的信号轨迹作为标志性构图。旧 Next 管理端可保留暖纸色，但两种界面共享相同的语义状态、可访问性、异步反馈和数据真实性要求；颜色令牌分别由各自全局 CSS 管理。

## 色彩令牌

| 角色 | CSS 变量 | 值 | 用途 |
|---|---|---:|---|
| 页面底色 | `--bg` | `#08060D` | 整体工作台底色 |
| 主面板 | `--panel` | `#121019` | 素材、预览和表单表面 |
| 次面板 | `--panel-2` | `#191622` | 工具条与分组 |
| 主文字 | `--text` | `#F5F1FF` | 标题与正文 |
| 次文字 | `--muted` | `#958BA9` | 辅助说明 |
| 细线 | `--line` | `#332B43` | 边框与分隔 |
| AI 强调 | `--purple` | `#8B5CF6` | 主按钮、焦点与识别状态 |
| 辅助识别 | `--cyan` | `#51D5FF` | 动作与口播证据 |
| 运行通过 | `--green` | `#35BD7D` | 通过与运行中 |
| 警示 | `--amber` | `#E5A93D` | 待复核 |
| 错误 | `--red` | `#EF665B` | 失败与阻断错误 |

对比约束：正文在 `--panel` 上使用 `--text`，紫色不承载正文；状态颜色不可成为唯一信息载体，必须同时有文字标签。

## 字体与排版

- 展示标题：Inter, `Segoe UI`, `Microsoft YaHei`, sans-serif；使用较紧字距和高字重表达技术感。
- 正文：Inter, `Segoe UI`, `Microsoft YaHei`, `PingFang SC`, sans-serif。
- 时间码与技术状态：`IBM Plex Mono`, `Cascadia Code`, monospace。
- 字号：12 / 14 / 16 / 20 / 28 / 40px。
- 正文行高 1.65，数据行高 1.4；长中文说明限制在 68 个汉字左右的视觉宽度。

## 间距、圆角与阴影

- 间距采用 4px 基准：4 / 8 / 12 / 16 / 24 / 32 / 48。
- 普通圆角 6px，登录主面板 18px，胶囊仅用于状态标签。
- 阴影只用于浮层、登录主面板和拖拽状态：`0 22px 60px rgba(0, 0, 0, .48)`。
- 表面层级主要靠细线、背景色和留白表达，不叠加多层卡片。

## 布局

- 宽屏：顶部产品导航 + 264px 素材栏 + 弹性剪辑区 + 326px 审核栏。
- 登录页：产品能力区与账号表单约 62:38；窄屏仅保留账号表单。
- 1200px 以下审核栏移至任务队列下方；760px 以下导航变为顶部，表格变为任务列表。
- 所有异步区域预留稳定高度，加载状态不得造成布局跳动。

## 组件语义

- 主按钮：每个区域最多一个，采用剪切强调色；忙碌时保留宽度并显示旋转图标。
- 次按钮：纸面底色加细线；危险操作不用主强调色。
- 输入框：始终有可见标签；错误信息与输入通过 `aria-describedby` 关联。
- 状态：`排队 / 识别口播 / 分析画面 / 生成剪辑方案 / 渲染成片 / 质量检查 / 已通过 / 待复核 / 失败`。
- 上传：同时提供拖放区与可见文件选择按钮，逐文件显示名称、大小和错误。
- Toast：用于短暂成功反馈；阻断错误必须保留在相关区域内。

## 动效

- 只使用 120–220ms 的颜色、位移与透明度过渡。
- 运行阶段可有单向扫描线，但 `prefers-reduced-motion: reduce` 时必须停用。
- 不对长任务显示伪造百分比；显示真实命名阶段和最后更新时间。

## 可访问性

- 可见键盘焦点为 2px 半透明紫色外环。
- 所有图标按钮必须有中文 `aria-label` 和可视/悬浮说明。
- 支持键盘选择文件、启动任务、切换筛选、翻页和选择任务。
- 触控目标不小于 44px；窄屏不产生页面级横向滚动。

## 令牌映射

本文件采用“现有运行时令牌为主”的所有权模型。`integration/suchen-ui/static/app.css` 的 `:root` 是桌面 SOCHEN 令牌唯一运行时来源，本文件镜像其已接受值；`web/app/globals.css` 独立拥有旧 Next 管理端令牌。组件只能通过 CSS 变量或已命名语义类使用共享值，`scripts/verify_design_tokens.py` 与严格界面审计负责检查漂移。
# 新版 Agent 单一入口

旧版访客码演示页、独立管理页及其样式和脚本已经移除。唯一产品入口是主登录页与黑紫色 Agent 工作台；账号、任务、知识库、计划版本、质检和负责人反馈统一使用主后端与数据库。
