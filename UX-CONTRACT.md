# 服临 AI 剪辑台 UX 合同

## Canonical UI Map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
|---|---|---|---|---|
| Select/Listbox | 原生 select | 本合同 + `UploadComposer.tsx` | native | 键盘选择 + 操作系统弹出菜单 |
| Form | `UploadComposer` 表单 | 本合同 | create | `noValidate` + 内联 API 错误 |
| Scrollbar | 全局样式 | `DESIGN.md` + `globals.css` | global | 标准属性 + WebKit 伪元素 |
| Toast | `ToastProvider` | 本合同 + `Toast.tsx` | success | `aria-live` + 自动/手动关闭 |
| SOCHEN knowledge form | `knowledge_form` | 本合同 + `integration/suchen-ui/static/app.js` | create/edit | `novalidate` + 内联错误 + busy |
| SOCHEN knowledge search | `loadKnowledge` | 本合同 + FastAPI `/api/knowledge` | query/category/enabled/page | 300ms + IME + abort |
| SOCHEN dialog | `openAppDialog` | 本合同 + `integration/suchen-ui/templates/index.html` | confirm/input/danger | focus trap + Escape + focus restore |
| SOCHEN connectivity | `connection_banner` | 本合同 + `renderKnowledgeSystem` | offline/retry | retained data + live status |
| SOCHEN authentication | `login.html` + FastAPI session middleware | SQLite `users` + signed session | login/register/logout | server route protection + inline errors |
| AgentVisitorForm | `agent.html` + `agent.js` | 本合同的受限访客合同 + `agent_demo.py` | private-code login / visitor upload | real HTTPS auth + quota + owner-boundary tests |

Next 管理端的按钮、输入和状态标签统一放在 `web/components/ui`；SOCHEN 内嵌页由 `integration/suchen-ui/static/app.js` 中的共享状态和 busy/dialog/toast 函数管理。图标统一使用 Lucide React；文件上传由原生 file input 与自有拖放区共同负责；视频预览明确使用 HTML5 原生媒体控件。永久删除知识条目必须使用应用内危险确认框，说明不可撤销，并保持弹窗直到服务器确认删除成功。

## 账号与权限合同

- 桌面和公网部署都必须登录后才能进入工作台；健康检查、静态资源、登录和注册接口是唯一匿名入口。
- 第一个成功注册的账号是管理员，后续账号是普通成员；密码只保存 PBKDF2 哈希，不写日志、不进入前端响应。
- 管理员可剪辑、查看记忆并新增、编辑、停用、删除和应用知识规则；普通成员可剪辑、查看记忆和查看知识规则，但不能改变公司标准。
- 会话保存在签名 Cookie 中，本机签名密钥写入运行数据目录并与源码分离；公网必须用服务器环境变量提供独立密钥并启用 HTTPS。
- 登录失败使用通用内联提示。会话过期的 API 返回 401，前端带当前安全路径回到登录页。
- 首个管理员必须在服务器本机回环地址注册，不能从局域网抢占管理员身份。

## 状态模型

剪辑任务只有以下状态：

- `queued`：已写入数据库，等待唯一工作线程。
- `running`：后台处理中，具体阶段由 `stage` 表示。
- `approved`：质量门禁通过，可预览或下载。
- `needs_review`：成片已生成但存在需人工确认的问题。
- `failed`：处理失败，展示后端返回的可操作错误。

命名阶段依次为 `transcribing → analyzing → planning → rendering → quality`。界面不从时间推测进度，不显示百分比。刷新后以 SQLite 状态恢复，不依赖浏览器内存。

## 上传合同

- 支持 MP4、MOV、MKV、AVI、M4V、WebM，单文件不超过 2GB。
- 空闲态同时展示拖放区和“选择视频”按钮。
- 选中文件后展示名称与体积；上传时禁止重复提交并提供明确状态。
- 客户端只做快速格式检查，服务端负责最终后缀、体积和路径边界检查。
- 上传成功后自动选中素材，但不会未经用户动作直接启动剪辑。
- 创建任务时可选 `AI 自动识别 / 单品 / 套装 / 大货`；自动识别只使用口播证据分类，页面随后展示实际命中的品类策略。
- 工作台默认把剪辑任务交由可审计 Agent 执行：真实调用 ASR、MediaPipe/OpenCV、规则检索、计划验证、FFmpeg 渲染和成片质检；失败最多自动修正两轮，仍不合格进入人工复核。原 `SOCHEN 1.2.3` 流程只保留为兼容与回归入口，不再是主按钮默认路径。
- 提交任务时读取 SOCHEN 当前保存的动作模板并原样传入，文件2的知识库和管理数据不覆盖该模板。
- 单条模式使用原生素材 select；批量模式使用可滚动复选框列表，最多一次提交 100 条。每个素材在服务端独立做重复任务检查，部分失败不回滚已成功入队的任务，并在反馈中报告跳过数量。

## 搜索与筛选合同

- 任务搜索匹配素材文件名，300ms 防抖。
- 中文输入法合成期间不触发请求；新请求发出时中止旧请求，过期结果不得覆盖新结果。
- 搜索词、状态筛选和页码同步到 URL，可刷新恢复、可前进后退。
- 清空按钮只在有内容时出现，且有 `aria-label="清空搜索"`。
- 状态筛选采用按钮组；原生 select 仅用于素材库选择，并明确接受跨平台菜单差异。
- SOCHEN 知识库搜索同样使用 300ms 防抖、中文输入法合成保护和 AbortController；分类、启用状态与分页均由后端执行，不能只在当前页面数组中假筛选。

## 异步与恢复合同

- 页面初次加载使用固定高度骨架，避免抖动。
- 存在活动任务时每 3 秒刷新；页面不可见时暂停轮询。
- 网络失败显示持久横幅，并保留上一次成功数据；恢复后提示已重新连接。
- SOCHEN 知识页每 15 秒刷新组件状态，页面不可见时暂停；原引擎、ASR、人体姿态与 SQLite 不得默认写成在线。
- 创建任务按钮防止重复提交；服务端也拒绝同素材的并行重复任务。
- 当前版本不宣称中途取消，因为底层渲染未提供安全取消点。
- Windows `SOCHEN-AI-Background` 用户登录任务持续运行统一服务；页面关闭不停止服务。主机开机并登录 Windows 后可供公司局域网访问，主机关机、休眠或局域网断开时其他设备不可访问。
- 断线时保留当前文字和审核输入，最多自动重试六次；“重新连接”、浏览器回到前台和网络恢复事件通过真实 API 重查服务，不重放剪辑或删除操作。

## 审核与预览合同

- 选中任务后，右栏显示来源、阶段、目标时长、创建时间和真实成片。
- 已生成报告时显示三段剪辑证据、构图缩放、质量指标和问题列表。
- 无结果、处理中、失败分别使用独立空态，不以空白区域代替。
- 下载使用普通链接语义；列表行选择使用按钮语义并保留可见焦点。

## 响应式合同

- 知识库与记忆页采用页面自然滚动、顶部导航固定；剪辑工作台保持原有内部滚动布局，切换页面时恢复各自的滚动所有权。
- 知识规则列表使用独立可聚焦滚动区，支持鼠标滚轮、触摸和键盘；搜索工具条与分页位于滚动区外。自动同步不替换相同列表、不清空表单，并保留列表滚动位置和焦点。
- 知识页“返回剪辑工作台”和左上角品牌返回同一剪辑工作台，不退出登录，不启动剪辑；知识库表单暂存于当前页面，往返后继续编辑。
- 各页面右上角“返回登录 / 注册”通过原 POST `/logout` 清除当前登录会话，返回黑紫色账号界面，登录和注册均可选择；有未保存修改时先用共享对话框确认。此操作不删除账号、知识库、视频，也不停止后台任务。工作台标题只显示“AI 智能剪辑”，不追加“首页”。

- `>= 1200px`：左侧导航、主工作区、右侧审核栏三列。
- `760–1199px`：导航收窄，审核栏在队列下方。
- `< 760px`：顶部导航，创建区、时间线、任务列表、审核区纵向排列。
- 移动端表格改为语义任务卡；所有主操作保持至少 44px 高。

## 技术栈透明度

架构区将组件分为“已接入”和“接口预留”。只有健康检查或代码路径真实存在的组件可以标记为已接入；Java/Spring Boot、PostgreSQL/MySQL、Redis、Dify/RAG、MCP 与剪映草稿在完成连接前只能显示为接口预留。

## 历史记忆合同

- `suchen-便携版-1.2.3` 以只读方式同步统计快照和逐条运行记录到当前 SQLite，不直接修改旧数据库；旧素材目录内仍存在的视频也进入统一素材选择器。
- 历史记录必须明确显示原片与成片当前是否存在；只保留数据库记录时不得提供失效的播放或下载操作。
- 旧库兜底硬剪、拒绝复核与未配对样本只能作反例或候选证据；只有“原片—人工合格成片”经负责人确认后才是金标准。
- 当旧动作模板与当前 SOP 冲突时，页面必须展示冲突，当前明确要求优先；本版本将“走到中央介绍商品”提升为 HARD。
- 页面不得把旧库中的 `quality_pass` 等同于人工验收通过。
# 淘宝女装 Agent 受限访客合同

演示变体：`agent.html` / `agent.js` / `agent.css`；Form使用命名业务变体 `AgentVisitorForm`，不是生产注册登录的替代实现。品牌与全局滚动条仍由 `app.css` 拥有。状态为持久行内反馈，不新增Toast队列。

访客使用负责人发放的数据库访问码与24小时签名HttpOnly会话；不创建公司管理员。任务按浏览器会话隔离，退出或清Cookie后无法恢复此前访客身份。公网仅允许HTTPS安全Cookie、同源POST。无需访问公司生产数据库。

独立SQLite任务库、原视频与成片在主程序的 `便携数据/面试演示` 内；专用原引擎在127.0.0.1:5002，独立SUCHEN_DATA_DIR和记忆库。原动作模板只复制一次，生产算法与配置不被改写。演示API仅允许自身上传/队列/报告/成片路由；无原后台代理、历史列表和删除API。

每条50MB、5分钟以内；MP4/MOV/M4V实际ffprobe校验，拒绝伪装的网络播放列表，最长边4096且不超过900万像素；全局最多20条、8条待处理、访客2条待处理；单worker。上传失败只删除本次尚未入队的不完整文件。成功素材不自动删除，访客明确同意本机存储、负责人手动清理；页面提示部分口播文字可能提交负责人已配置的AI服务，禁止敏感素材。服务重启把未完成任务标记失败，不自动重复执行。刷新3秒、隐藏暂停、离线保留结果，不重放上传。

退出访问不会删除素材；已选择未提交的文件仍留在表单，重新进入可继续提交。处理耗时真实记录，未保证2分钟。成片按品类30/60/120秒及3:4检查；连续硬剪兜底或缺乏五阶段顺序与尺码、面料结构化证据强制needs_review，即使原报告称通过也不算合格；未知证据格式不能猜测为通过。原引擎检查与人工验收分开；首帧、颜色、口播与Y500校准未宣称全部自动验收。

## 本机访问码管理合同（2026-09-14）

业务来源：用户本轮明确要求负责人自行设置并发放访客访问码、数据库保存使用记录、负责人可以查看数据库。没有新增付费、硬删除、实名认证或公网管理员权限。

命名业务变体 `OwnerAccessForm` 和 `OwnerInlineRevocation` 复用 `AgentVisitorForm` 的黑紫令牌、品牌、自然文档滚动、44px按钮、掩码/显示和持久行内反馈；不加载生产业务 `app.js`，不新增Toast。停用/启用在记录内部二次确认，显示客户备注、影响范围、可恢复性；取消初始焦点，失败留在确认区域，服务端成功才更新。每次状态修改带版本号，冲突409要求刷新。不硬删除访问码或历史视频。

`agent_admin.py` 独立绑定127.0.0.1:8084，拒绝非本机Host、验证同源POST和本机管理登录码。管理凭据不提供给访客。公网8082没有管理路由，数据库列表不会返回公司数据、密文、解密密钥或文件路径。管理会话8小时，服务重启失效。权限由服务端校验，不能仅依靠隐藏按钮或代理IP。

访问码在隔离的 `访客任务.sqlite3` 内以Fernet密文保存，HMAC用于查找，密钥来自本机受限访问配置。仅负责人明确点击显示时返回码值；不写浏览器存储、URL、日志、Toast。原配置访问码只迁移一次，重启不能恢复已停用码。每次成功登录记录事件；拒绝记录错误尝试的明文码。停用撤销所有关联会话，重新启用必须重新登录。同一访问码不同浏览器保持不同任务所有者；客户备注不是验证过的个人身份。

记录类型为访问码、成功进入事件、关联任务，服务端每页20条，边界按钮禁用、总数真实。列表自然高度，切换不继承固定表格高度。刷新保留旧记录并丢弃过期响应；网络失败保留输入、不自动重放权限写入。创建码唯一约束防止不确定请求重复创建。401清除敏感列表并返回本机登录。没有持久保存含密钥的浏览器草稿。创建后清空码，返回访问码列表；离开有未保存输入使用窄范围beforeunload提醒。

## 访问申请与公网管理（2026-09-14，本轮用户决定）

用户要求Web客户入口、负责人发放授权码、本机运行且异地访问。公开首页允许查看产品说明和提交申请，不允许未授权使用剪辑；申请编号与访问码不同。`VisitorApplicationForm` 复用访客表单令牌、行内校验与无原生校验泡的行为。登记公司或团队名称、联系人姓名、联系方式、可选职位或部门及申请用途，申请人确认本机保存，不收身份证或密码。申请不会自动授权；负责人核对申请编号后手动审批并发放访问码，不自动发送外部消息。

申请保存到隔离SQLite，状态pending→approved；审批与访问码创建同一事务，唯一约束及版本校验防止重复发码。单浏览器签名HttpOnly申请凭证七天，刷新恢复自己的编号与状态，不公开申请列表或返回访问码。匿名申请限4096字节、每分钟全局10次、总数1000。负责人列表每页20条、真实记录、无删除操作。

负责人后台“客户访问申请”按申请时间倒序分页读取 SQLite，完整显示登记字段、申请编号、申请时间、审批状态及关联访问码状态。旧版申请保留，缺失字段明确显示“旧版申请未填写”。联系方式只向通过独立管理登录码认证的负责人返回，不进入客户状态接口、URL或日志。

本轮明确允许负责人管理Web页面提供公网地址 `/manage/`，替代此前“没有公网管理入口”的部署限制；数据库边界不变。仅负责人独立管理码可登录，访客码无法登录管理页。公网管理使用HTTPS、Secure HttpOnly且Path=/manage的独立Cookie、独立签名salt和同源POST；不能复用访客会话。登录前仅提供管理登录页，不提供客户列表、访问码、用途或数据库。原127.0.0.1:8084本机管理仍保留。管理数据不包括生产公司数据库。

免费公网地址是否可从异地访问，必须另行实测；本机经过代理200不视为外部验收。已知代理超时与直连连接重置不能用忽略TLS、关闭认证或开启远程桌面来绕过。未核实异地可达时明确报告部分完成，不承诺固定免费域名。

## 邀请码简化流程（2026-09-16，用户新决定）

客户公网入口只保留一个邀请码登录表单；邀请码即访问授权码，不再展示企业登记、申请编号或在线申请流程。负责人在线下确认客户后，在本机管理页创建邀请码并直接发送给客户。原申请数据库记录保留但不在客户入口展示。

桌面“访客访问码管理”连接仅绑定 `127.0.0.1:8084`，打开页面时自动建立8小时本机负责人会话并直接进入功能区，不再要求负责人重复输入管理登录码。公网 `/manage/` 仍保留独立管理登录认证，避免客户通过共享网址进入负责人数据。

## 直接进入剪辑工作台（2026-09-16，用户最终决定）

AI智能剪辑客户入口取消邀请码和登录表单，打开页面后直接展示上传、任务与成片工作台。服务器在首页响应中自动签发24小时 HttpOnly 会话；不同浏览器仍使用不同任务所有者，不能查看彼此的视频。公网继续要求 HTTPS Secure Cookie、同源写入、上传配额和任务容量限制。邀请码及历史访问记录保留在数据库中，但不再作为进入剪辑功能的前置条件。
